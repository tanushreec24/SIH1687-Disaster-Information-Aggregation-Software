import Content from '../Content';
import MapComponent from './MapComponent'; // Make sure the path is correct

export default function Home() {
     return (<Content />);
     return (<MapComponent />);
     
     
    
  }